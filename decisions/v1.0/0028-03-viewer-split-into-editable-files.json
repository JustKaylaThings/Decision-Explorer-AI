{
  "id": "d28.3",
  "title": "Viewer split into editable files",
  "category": "Architecture",
  "phase": "Implementation",
  "type": "technical",
  "version": "1.0",
  "question": "Should the viewer stay one big generated file, or split into separate HTML/CSS/JS that read the decision data?",
  "status": "decided",
  "supersedes": [
    "d28.2"
  ],
  "date": "2026-06-22T11:33:30",
  "rationale": "Made the viewer ONE shared template — each project now symlinks to a single canonical copy, so editing it once updates every project automatically and only the decision data stays per-project (see d35). The split-file, read-JSON-live model is unchanged; what changed is per-project copies → one shared, self-healing link.",
  "options": [
    {
      "id": "o28c",
      "label": "Separate files that read the JSON directly (local server)",
      "chosen": true,
      "tradeoffs": [
        {
          "criterion": "editing & comparing",
          "sentiment": "+",
          "note": "restyle in plain CSS; edit a decision's JSON and refresh, no rebuild"
        },
        {
          "criterion": "no duplicated data",
          "sentiment": "+",
          "note": "the page reads each decision file; nothing copies the content"
        },
        {
          "criterion": "works offline",
          "sentiment": "~",
          "note": "still offline, but the folder must be served locally (python3 -m http.server)"
        },
        {
          "criterion": "manual effort",
          "sentiment": "~",
          "note": "start a small local server to view; no rebuild for content edits"
        }
      ]
    },
    {
      "id": "o28a",
      "label": "Separate index.html / styles.css / app.js + generated data.js",
      "chosen": false,
      "tradeoffs": [
        {
          "criterion": "editing & comparing",
          "sentiment": "+",
          "note": "restyle in plain CSS; no Python, no regenerating the look"
        },
        {
          "criterion": "works offline",
          "sentiment": "+",
          "note": "data is a script include, so double-click still works with no server"
        },
        {
          "criterion": "no duplicated data",
          "sentiment": "-",
          "note": "data.js holds a baked copy of every decision, separate from the JSON"
        }
      ]
    },
    {
      "id": "o28b",
      "label": "Keep the single generated file (leave as is)",
      "chosen": false,
      "tradeoffs": [
        {
          "criterion": "editing & comparing",
          "sentiment": "-",
          "note": "styling is buried in the Python; every change regenerates the whole file"
        },
        {
          "criterion": "build effort",
          "sentiment": "+",
          "note": "already built, nothing to change"
        }
      ]
    }
  ]
}
